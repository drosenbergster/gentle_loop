# Optional for local Metro dev — app uses __DEV__ fallbacks if unset.
# Required for EAS builds (values come from eas.json profiles).
# Copy to .env.local and fill in if you want to override.

EXPO_PUBLIC_API_PROXY_URL=https://amghuhcisazsxsqrrmep.supabase.co/functions/v1
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
